SELECT 
	trip_id
FROM 
	trips
WHERE 
	bike_id > 500
ORDER BY duration

SELECT 
	*
FROM 
	stations
WHERE 
	station_id = '84'
	
SELECT 
	min_temp
FROM 
	Occurrences Rain
WHERE 
	zip_code = '94301'